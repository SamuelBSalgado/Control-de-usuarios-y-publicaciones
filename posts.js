// Conjunto de datos de posts
class Posts {
    constructor(posts){
        this.postsData = posts;
        this.lastPostID = posts.length+1;
    }

    searchId(userId){ //Buscar id de usuario y devolver todas las publicaciones.
        
    }

    searchPost(postId){ //Buscar id de publicación y devolver esa publicación.
        
    }
}
